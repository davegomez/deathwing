import createReducer from '../utils/create-reducer';

const initialState = [];

// TODO: Document the structure of each retro here... the structure is complex
export default createReducer({

}, initialState);
